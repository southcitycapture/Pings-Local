[default]
description = "Push registration, token readback, badge counts, and push events."
permissions = [
  "allow-request-push",
  "allow-get-token",
  "allow-set-badge",
  "allow-register-listener",
]
